import React from "react";
import { observer } from "mobx-react-lite";
/* import getPersons from "../modules/persons"; */

const LoadPersonFromServer = () => {
  /*  const loadPersons = async () => {
    try {
      //simulate fetching data from server

      const data = await getPersons();
      console.log(data);

      // update mobx store with fetched data
    } catch (error) {
      console.log("Error loading persons from the server", error);
    }
  }; */

  return <div></div>;
};

export default observer(LoadPersonFromServer);
